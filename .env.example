# Copy to .env and set your key (never commit .env)
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Planner: real LeWM checkpoint is default when checkpoints/lewm_flight_v1.pt exists
# SKYMIND_MOCK_PLANNER=1
# SKYMIND_PLANNER_DEVICE=cpu
